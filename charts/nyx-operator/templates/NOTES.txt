nyx-operator has been deployed.

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

The nyx-operator controller manager is now running and watching for nyx.ai CRDs.

Check operator status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "nyx-operator.name" . }}
