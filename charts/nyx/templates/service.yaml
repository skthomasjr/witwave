{{- range .Values.agents }}
---
apiVersion: v1
kind: Service
metadata:
  name: nyx-{{ .name }}
  labels:
    {{- include "nyx.agentLabels" .name | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: {{ .name }}
  ports:
    - name: http
      port: 8000
      targetPort: 8000
{{- end }}
