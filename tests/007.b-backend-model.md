---
description: Verifies that requests are routed to the correct agent and model as configured in backend.yaml.
enabled: true
---

Bob's `backend.yaml` routes all concerns to the `codex` agent with model `gpt-5.1-codex`. This test verifies both the agent and the model by inspecting the backend's own logs after sending a fresh A2A request.

## Setup

Send a fresh A2A request to bob's nyx agent to produce a new trace entry:

```
curl -s -X POST http://localhost:8099/ \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":"007b","method":"message/send","params":{"message":{"role":"user","messageId":"msg-007b","contextId":"routing-model-check","parts":[{"kind":"text","text":"Respond with MODEL_CHECK_OK."}]}}}'
```

Wait for the response to contain `MODEL_CHECK_OK` before proceeding.

## Verification

**Agent check** — confirm the request was handled by the codex backend (not claude):

- `.agents/test/bob/a2-codex/logs/conversation.log` must contain `MODEL_CHECK_OK`
- `.agents/test/bob/a2-claude/logs/conversation.log` must NOT contain `MODEL_CHECK_OK`

**Model check** — confirm the correct model was used:

Parse `.agents/test/bob/a2-codex/logs/trace.jsonl` (one JSON object per line). Find the entry whose `session_id` corresponds to the `routing-model-check` context. The `model` field must equal `gpt-5.1-codex`.

Since `routing-model-check` is not a UUID, the nyx-agent derives a deterministic UUID via `uuid5(NAMESPACE_URL, "routing-model-check")`. Compute that value and match it against the trace entries:

```
python3 -c "import uuid; print(uuid.uuid5(uuid.NAMESPACE_URL, 'routing-model-check'))"
```

Then grep the trace log for that session_id and verify the model field:

```
grep <computed-session-id> .agents/test/bob/a2-codex/logs/trace.jsonl
```

## Pass/Fail Criteria

The test passes if:
1. The A2A response contains `MODEL_CHECK_OK`.
2. `MODEL_CHECK_OK` is present in `.agents/test/bob/a2-codex/logs/conversation.log`.
3. `MODEL_CHECK_OK` is NOT present in `.agents/test/bob/a2-claude/logs/conversation.log`.
4. The trace entry for the session has `"model": "gpt-5.1-codex"`.

The test fails if any of the above conditions are not met.

**If the failure is caused by a code bug in the system under test, do not fix it — mark the test as failed and report the issue. Only fix tooling or execution problems that prevent the test itself from running.**
