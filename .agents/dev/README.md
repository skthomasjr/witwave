# Dev Agents

This directory contains agents used for development purposes — iterating on agent configuration, testing new
behaviors, and validating changes before promoting to active agents.

Each agent directory contains a `.nyx/` directory for nyx-harness runtime config and a `.claude/` directory for the
Claude backend instance (identity, skills, and behavioral instructions). See `AGENTS.md` at the repo root for the full
layout.
