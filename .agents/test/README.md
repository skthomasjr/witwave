# Test Agents

This directory contains agents used for testing purposes — validating agent behavior, verifying configuration changes, and exercising the agent runtime without affecting active development agents.
