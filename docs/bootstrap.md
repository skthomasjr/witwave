# Bootstrap Docs

Bootstrap walkthroughs now live with the agent teams they deploy:

- Self-maintaining team: [`.agents/self/bootstrap.md`](../.agents/self/bootstrap.md)
- Test/smoke team: [`.agents/test/bootstrap.md`](../.agents/test/bootstrap.md)

Keeping these under `.agents/` makes the deployment instructions follow the same self-vs-test split as the agent configuration itself.
