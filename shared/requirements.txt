# Shared library used by harness, backends, and MCP tools. Install
# matches the runtime surface (PYTHONPATH=shared) so its tests resolve
# the same way they do in the deployed images. Versions track the
# harness Dockerfile to avoid drift.
httpx==0.28.1
prometheus-client==0.24.1
pyyaml==6.0.2
starlette==0.49.1
